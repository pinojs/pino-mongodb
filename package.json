{
  "name": "pino-mongodb",
  "version": "0.2.0",
  "description": "Load pino logs into MongoDB",
  "main": "index.js",
  "scripts": {
    "test": "clear && mocha --opts mocha.opts",
    "build": "babel src --out-dir dist --presets=latest --plugins=babel-plugin-transform-runtime",
    "dev": "npm run build -- --watch",
    "postversion": "git push & git push --tags"
  },
  "bin": {
    "pino-mongodb": "index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ViktorKonsta/pino-mongodb.git"
  },
  "keywords": [
    "pino",
    "mongo",
    "mongodb",
    "pino-mongodb"
  ],
  "author": "ViktorKonsta",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ViktorKonsta/pino-mongodb/issues"
  },
  "homepage": "https://github.com/ViktorKonsta/pino-mongodb#readme",
  "dependencies": {
    "commander": "^2.9.0",
    "mongodb": "^2.2.11",
    "babel-runtime": "^6.18.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-polyfill": "^6.16.0",
    "babel-preset-latest": "^6.16.0"
  }
}
